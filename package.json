{
  "name": "@openakb/spec",
  "version": "0.0.0",
  "private": true,
  "description": "OpenAKB specification monorepo (workspace root; not published).",
  "type": "module",
  "scripts": {
    "check:schema": "node scripts/ci/validate-schema.mjs",
    "check:conformance": "node scripts/ci/check-conformance.mjs"
  },
  "devDependencies": { "ajv": "^8.17.1", "ajv-formats": "^3.0.1" }
}
