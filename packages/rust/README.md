# openakb-validate

`openakb-validate` is a pure-library Rust validator for OpenAKB descriptor documents. It provides schema, descriptor-local semantic, strict-profile unknown-core-property validation, and opt-in async content checks with stable diagnostic codes.

Status: pre-release.
