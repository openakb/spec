An inline note <!-- draft -- revisit [cite: alpha] --> keeps its marker: the double dash in its body makes it an invalid CommonMark comment, so the whole run is literal text.

A well-formed inline comment <!-- hidden [cite: beta] --> masks the marker it wraps.

A trailing real citation [cite: gamma] closes the fixture.
